#include <iostream>
#include <fstream>
#include <vector>

using namespace std;


int main() {
    srand(time(NULL));
    ofstream zapisovac;
    zapisovac.open("day_calendar.unl");
    zapisovac.close();
    return 0;
}

